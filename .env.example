# Copier en .env et adapter — voir README.md.

VITE_API_URL=http://localhost:3001

# Sentry (suivi d'erreurs frontend) — voir src/instrument.js. Sans cette clé, Sentry est
# désactivé silencieusement (l'app démarre et fonctionne normalement).
VITE_SENTRY_DSN=
